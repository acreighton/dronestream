import React, { Component } from 'react';

var ChartApp = React.createClass({
    render: function() {
        return (
            <svg id="chart"></svg>
        );
    }
});

module.exports = ChartApp;
